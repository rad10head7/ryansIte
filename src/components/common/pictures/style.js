import Img from "gatsby-image";
import styled from 'styled-components';

export const MultiPics =styled(Img)`
  display: flex;
  align-items: space-between;
  border-radius: 5px;
  box-shadow: 0px 0px 64px rgba(0, 0, 0, 0.15);
  width: 100%;
`